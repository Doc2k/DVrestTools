{% if inhalte is not null %}
  {% set Zaehler=0 %}
  {% for item in inhalte %}
    {% if(loop.index0==0) %}
      {{ callb }}({'Name': '{{ item.itemDescription.name1 }}', 'Warehouse' : '{{ item.variationStock.warehouseId }}'
    {% endif %}
     ,'{{ item.variationBase.id }}' : {'nettoStock' : '{{ item.variationStock.stockNet }}', 'physStock' : '{{ item.variationStock.stockPhysical }}'}
  {% endfor %}
  })
{% else %}
  Nanü
{% endif %}
