

      {% if currentItems is not null %}
        {% for item in currentItems %}
        {{ callb }}({'Name': '{{ item.itemDescription.name1 }}', 'phys' : '{{item.variationStock.stockPhysical}}', 'netto' : '{{item.variationStock.stockNet}}'})
      {% else %}
              Nanü
      {% endif %}
