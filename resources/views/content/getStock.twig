{% if inhalte is not null %}
  {% for item in inhalte %}
    {{ callb }}({'Name': '{{ item.itemDescription.name1 }}', 'physicalStock' : '{{ item.variationStock.stockPhysical }}', 'nettoStock' : ''})
  {% endfor %}
{% else %}
        Nanü
{% endif %}
