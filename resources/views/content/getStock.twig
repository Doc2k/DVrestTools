{% if inhalte is not null %}
  {% for item in inhalte %}
    {{ callb }}({'Name': '{{ item.itemDescription.name1 }}', 'physicalStock' : '{{ item.variationBase.id }}', 'nettoStock' : ''})
  {% endfor %}
{% else %}
        Nanü
{% endif %}
